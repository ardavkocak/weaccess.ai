requests>=2.31.0
beautifulsoup4>=4.12.0
openpyxl>=3.1.0
lxml>=4.9.0
flask>=3.0.0
gspread>=6.0.0
google-auth>=2.28.0
python-dotenv>=1.0.0

# JS ile yüklenen sayfalar için Playwright/Chromium desteği.
# ÖNEMLİ: Bu sürüm, Dockerfile'daki
# "mcr.microsoft.com/playwright/python:vX.Y.Z-jammy" imaj etiketiyle BİREBİR
# aynı olmalı. Farklı sürümler farklı Chromium build'i bekler ve imajdaki
# önceden kurulu tarayıcı bulunamaz hatası verir.
playwright==1.42.0
gunicorn>=21.2.0
