Django>=5.0,<5.2
python-dotenv>=1.0.0
gunicorn>=22.0,<23.0
whitenoise>=6.6,<7.0

# --- Discord Bot entegrasyonu: container Baslat/Durdur/Yeniden Olustur ---
docker>=7.1,<8.0

# --- Zimmet Sistemi entegrasyonu (Faz 2) ---
psycopg2-binary>=2.9,<3.0
Pillow>=10.3,<11.0
openpyxl>=3.1,<4.0
reportlab>=4.1,<5.0
Faker>=24.0,<25.0

# --- Ofis Gorev Takibi + Yemek Sistemi (Faz 3) ---
# (ek bagimlilik yok; openpyxl yukarida zaten var)

# --- Dokumantasyon Otomasyonu (Faz 3) ---
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
gspread>=6.0.0
google-auth>=2.28.0

# --- Aylik Takip (Faz 3) ---
pdfplumber>=0.11.0
pytesseract>=0.3.10
python-docx>=1.1.0

# --- IK Otomasyonu: coklu-motor PDF ayristirma pipeline'i (Faz 4) ---
# pdfplumber (yukarida) -> camelot -> tabula -> PyMuPDF -> OCR (tesseract) sirasiyla denenir.
PyMuPDF>=1.24.0
camelot-py[cv]>=0.11.0
tabula-py>=2.9.0
pdf2image>=1.17.0
rapidfuzz>=3.9.0
